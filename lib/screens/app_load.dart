import 'package:flutter/material.dart';

class AppLoad extends StatelessWidget {
  const AppLoad({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
